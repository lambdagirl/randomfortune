import React from 'react';
import ReactDOM from 'react-dom';
import Lucky from './lucky';


document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  ReactDOM.render(<Lucky />, root);
});
